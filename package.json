{
  "name": "aionmanger_server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "nodemon ./bin/www",
    "test": "NODE_ENV=test mocha -R spec --recursive",
    "db:migrate": "sequelize db:migrate",
    "db:migrate:test": "sequelize --env test db:migrate"
  },
  "dependencies": {
    "bcrypt": "^0.8.2",
    "body-parser": "~1.0.0",
    "cookie-parser": "~1.0.1",
    "debug": "~0.7.4",
    "express": "~4.2.0",
    "lodash": "^3.7.0",
    "morgan": "~1.0.0",
    "pg": "^4.3.0",
    "pg-hstore": "^2.3.2",
    "sequelize": "^2.1.0"
  },
  "devDependencies": {
    "chai": "^2.3.0",
    "supertest": "^0.15.0"
  }
}
